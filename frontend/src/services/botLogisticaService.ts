/**
 * Fachada del bot logístico. La implementación vive en ./botLogistica,
 * organizada por capacidad (NLP, direcciones, cargas, tarifas, asignaciones
 * y conversación). Este archivo conserva la ruta de import histórica.
 */

export * from './botLogistica';
